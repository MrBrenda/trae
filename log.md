# monitorda 开发日志

**项目**：排水管网在线监测数据自动化诊断方法与实践  
**目标**：会议汇报（2026年7月，主题：城市给排水管道更新与低碳运维）  
**记录时间**：2026-06-12

---

## 一、本阶段完成工作

### 1. Streamlit 数据看板（`app/streamlit_app.py`，~770行）

为会议演示构建了完整的 Web 交互看板，共 5 个标签页：

| 标签页 | 功能 |
|--------|------|
| 📊 数据总览 | 节点选择器（26节点）、液位/流量时间序列图、QC标志分布饼图 |
| 🌧 降雨事件 | 事件甘特时间轴、5项KPI卡片（含覆盖雨量站数）、事件明细表 |
| 📈 指标分析 | 节点×事件热力图（升幅/时滞/半衰期）、单节点事件详情图 |
| 🔍 诊断结果 | 污水节点RDII分级卡片（High/Medium/Low/NA）、分级彩色明细表、升幅条形图；雨水节点运行状态 |
| ⚙️ 流水线 | 六阶段一键运行控制台 |

关键设计决策：
- `get_site_cfg()` 直接读 YAML 而不走 `lru_cache`，避免 Streamlit 进程内 cache 与文件修改脱节
- `node_label()` 展示拼音缩写（保密协定），回退到中文名
- 诊断结果以污水节点为重点，RDII 分级作为一级输出；雨水节点单独分区

启动方式：
```bash
make app          # .streamlit/config.toml 配置免交互启动
# 或
echo "" | STREAMLIT_BROWSER_GATHER_USAGE_STATS=false \
  .venv/bin/streamlit run app/streamlit_app.py --server.headless true
```

---

### 2. 数据入库重构（`src/monitorda/ingest.py`）

原有入库逻辑仅支持 `data/raw/nodes_flow/` 和 `data/raw/nodes_level/` 两个目录，无法处理实际数据。

重构后支持：
- `data/raw/nodes/污水/`→ `nodes_sewage` 类型
- `data/raw/nodes/雨水/`→ `nodes_stormwater` 类型
- **双行表头 Excel**：第0行为位置名称合并单元格，第1行为列名（`header=1`，位置名从 row0 col0 提取）
- **流量+液位同文件**：`load_node_combined()` 从同一 Excel 抽取两类数据
- **路名反查节点ID**：`_location_index()` 建立 `{路名/拼音 → node_id}` 反向映射，支持中文文件名识别节点
- **m³/h 单位自动换算**：`_pick_flow_with_unit()` 检测列名中的单位字符串，除以3600转换为 m³/s
- **气象局降雨格式**：检测年/月/日/时独立列（`_is_meteo_format()`），合并为 timestamp；站号带 `V-` 前缀

---

### 3. 降雨事件识别重构（`src/monitorda/events.py`）

**核心变更：跨站统一事件**

原逻辑：每个雨量站独立识别事件，导致同一场降雨产生 5 条记录（5个站），事件数虚高。

新逻辑（三步）：
1. 逐站识别 → `per_station` 列表
2. **跨站时序合并**（sweep-line）：时间窗口重叠或间隔 ≤ `merge_gap_h=6h` 的不同站事件合并为一场
3. 统一编号：`E{YYYYMMDD}-{seq:02d}`，代表站取总雨量最大的站

输出新增 `n_stations` 列（参与本场次的雨量站数），可反映降雨空间分布均匀性。

**关键 Bug 修复（本次回归调试中发现）**：

> 数据文件只记录有雨时刻（稀疏存储），连续两行之间可能存在数百小时的时间跳跃。原算法在扫描"wet run"时，只要下一行是 wet hour 就直接并入，忽略时间距离，导致 `V8870` 站把 9月16日和10月11日的两场独立降雨合并成一个持续近一个月的"事件"（`2025-09-16 → 2025-10-12`）。

修复：在内层 while 循环中加入提前退出条件——距上次湿润时刻超过 `merge_gap_h` 即视为新 run，无论下一行是否为 wet。

```python
# 修复前：
if wet[j + 1]:
    last_wet = j + 1; j += 1

# 修复后（先检查时间跳跃）：
if gap_h > merge_gap_h:
    break
if wet[j + 1]:
    last_wet = j + 1; j += 1
```

---

### 4. 指标计算重构（`src/monitorda/compute.py`）

原逻辑：每个节点通过 `station_for_node()` 查找对应雨量站，再查该站的事件列表。

新逻辑：跨站合并后所有节点共用同一张全区统一事件表（`all_events`），移除逐站查找。

---

### 5. 诊断逻辑重构（`src/monitorda/diagnose.py`）

**输出变更**：`classify()` 返回值从 3 元组改为 4 元组：`(rdii_grade, category, evidence_score, notes)`

**诊断重心调整**：
- 污水节点为诊断重点，RDII 分级（High/Medium/Low/NA）作为一级输出
- 雨水节点侧重运行状态（响应强 vs 雨水管低效）

**分类规则（污水节点）**：

```
优先级 1 — 直连（雨水直接接入）：
  时滞中位值 ≤ 60min（10min 数据分辨率限制，物理< 2min 无法精确测量）
  + 升幅 ≥ 1.0m

优先级 2 — 混接（雨污混接，入流主导）：
  任意事件升幅 ≥ 3.0m（peak_is_high）
  或中位升幅分级为 High

优先级 3 — 入渗（地下水渗入）：
  升幅非 High + 半衰期中位值 ≥ 18h

优先级 4 — 未定 / 数据不足
```

**雨水节点规则**：
- rdii_grade ∈ {Low, NA} → 雨水管低效（管道无雨水汇入响应，疑已截流至污水系统）
- rdii_grade ∈ {Medium, High} → 运行正常

**新增 `rise_amp_max`**：聚合时新增最大升幅字段，解决"多场事件中仅1-2场达到 High，中位值被拉到 Medium"导致的误分类问题。

---

### 6. Schema 更新（`src/monitorda/schema.py`）

- `EVENTS`：新增 `n_stations` 列（`int, ge(1)`）
- `NODE_DIAGNOSTICS`：新增 `rdii_grade` 列（位于 `mean_rise_amp_m` 之前）、`pinyin` 字段

---

### 7. 配置更新（`config/`）

**`config/sites.yaml`**：扩充至 26 个监测节点

| 节点类型 | 编号 | 数量 |
|---------|------|------|
| 官方雨水节点 | S01–S10 | 10 |
| 扩展雨水节点 | S11–S12 | 2 |
| 官方污水节点 | W11–W19 | 9 |
| 扩展污水节点 | W20–W24 | 5 |

每个节点新增 `pinyin` 字段（路名拼音缩写，保密协定），用于看板展示和文件名匹配。

**`config/settings.yaml`**：调整 5 项阈值

| 参数 | 旧值 | 新值 | 原因 |
|------|------|------|------|
| `events.min_event_duration_h` | 2.0h | 1.0h | 允许单小时短历时强降雨（V8872 7/19 7.6mm 仅1小时） |
| `bwf.min_samples` | 144 | 36 | 兼容 stuck0 节点（W13 管道长期干燥，好数据稀少） |
| `diagnose.direct_connection_lag_min` | 5min | 60min | 10min 数据分辨率下最短可测时滞约 45min |
| `diagnose.infiltration_halflife_h` | 20h | 18h | W17 实测 19.04h，低于旧阈值 1h 无法识别 |
| `diagnose.inflow_halflife_h` | 8h | 8h（不变） | - |

---

### 8. 基础设施

- `.streamlit/config.toml`：`gatherUsageStats=false`，免去交互式邮件提示
- `pyproject.toml`：新增 `[app]` 可选依赖组（`streamlit>=1.36`, `plotly>=5.22`）
- `Makefile`：新增 `app-install` 和 `app` 目标

---

## 二、当前已知问题

### P0 — 数据质量问题（影响诊断可信度）

**① W20–W24、S11、S12、S06 数据极度稀疏**

这 7 个节点 good 数据量极少（6–304 行），所有事件指标均为 NaN，诊断结果为"未定"或"雨水管低效"，但缺乏实测支撑：

| 节点 | good行数 | 问题 |
|------|---------|------|
| S12 | 6 | 几乎无有效数据 |
| W24 | 13 | 几乎无有效数据 |
| S06 | 34 | 数据起始于 2026-02，事件窗口覆盖差 |
| W21 | 232 | 稀疏 |
| S04 | 243 | 稀疏 |
| W20 | 288 | 稀疏 |
| S11/W22/W23 | 304–312 | 稀疏 |

根本原因：相关传感器长期 `stuck0`（管道干燥或设备故障），QC 模块将连续0值标记为 stuck0 并从后续计算中排除。

**② W15 半衰期异常（484h、1059h）**

W15 在 E20250916 和 E20251010 事件后的recession window（48h）内仍有继发性液位抬升（来自9月17-19日持续的小幅降雨），导致指数衰减拟合给出数百小时的半衰期。目前通过 `rise_amp_max` 规则仍正确分类为 混接，但这一异常值对其他节点有潜在干扰风险。

**③ 半衰期对多次降雨的鲁棒性不足**

`recession_halflife` 的计算假设事件结束后液位单调衰减，但实际数据中常出现次级降雨抬升。需要在拟合前对 recession 窗口内的异常抬升做过滤。

---

### P1 — 诊断准确性问题（与 0423 报告的偏差）

**① W16 被判"未定"，0423 预期"入渗+入流混合"**

W16 rise=0.54m（勉强超 Low 阈值 0.5m），rdii_grade=Low，halflife=15.6h（< 18h 入渗阈值）。既不满足 Low（雨水管低效）也不满足入渗，落入"未定"。

0423 对 W16 的描述："中低；入流+入渗混合"—— 这是一种混合模式，当前分类体系无法表达。

**② W19 直连的证据强度偏低**

W19 median lag=0.875h（52.5min）勉强满足 60min 阈值，rise=1.67m（Medium）。直连判定通过，但置信分 0.90 可能虚高——仅有 1/4 场次表现出真正短时滞（E20250916: lag=0.75h），其余场次均超过 1h，这更可能是混接而非直连。

**③ W11 响应异常弱**

W11 mean rise=0.04m，几乎为零响应。节点位于北二环辛家庙立交，若传感器正常则管网确无外来水入侵，但也需核实设备是否正常工作。

---

### P2 — 工程质量问题

**① `stuck0` 策略需精细化**

当前 QC 将连续6小时读0定义为 `stuck0` 并排除。对于雨水节点（旱季管道确实为空），这会错误过滤大量合法的干天基线数据，导致 BWF 样本不足。建议按节点类型差异化处理：
- 雨水节点：旱季0值保留为 `good`（管道确实为空）
- 污水节点：连续0值仍视为 `stuck0`（污水管不应为空）

**② recession_halflife 计算对异常抬升无防护**

当前实现直接对 `t_end` 后 48h 的数据做指数拟合。若其中存在次级降雨抬升，拟合结果极不稳定（W15 出现 1059h）。

**③ 事件识别阈值的空间不均匀性**

当前 `min_event_total_mm`、`min_event_duration_h` 是全局参数。雨量站空间分布和降雨空间不均匀性（如仅 V8872 有的 7/19 局地暴雨）对识别结果影响较大。后期可引入 Thiessen 权重降雨。

---

### P3 — 功能缺失

**① `report` 阶段尚未运行**

整个流水线的最终输出（`reports/<date>/report.md`, `report.docx`, `figures/`）尚未生成。需 `make run` 完整跑一遍。

**② Streamlit 看板尚未经过完整演示测试**

Tab 3（指标分析）的热力图和单节点详情图在数据完整性方面未做边界测试；当 rdii_df 为空或节点无数据时的降级展示逻辑未充分验证。

---

## 三、当前管道运行状态

```
✅ ingest   → 26 节点入库，5 雨量站（V8805/8870/8872/8874/8883）
✅ clean    → QC 标志就位（good: 59,351条 / stuck0: 23,884条）
✅ events   → 5 场降雨事件识别完成（符合 0423 报告基准）
✅ metrics  → 26节点×5事件 = 130 行 RDII 指标
✅ diagnose → 26节点诊断完成
❌ report   → 未运行
```

**事件列表**：

| 事件ID | 开始时间 | 累计雨量 | 覆盖站数 |
|--------|---------|---------|---------|
| E20250614-01 | 2025-06-14 03:00 | 9.5mm | 5 |
| E20250719-01 | 2025-07-19 18:00 | 33.0mm | 5 |
| E20250916-01 | 2025-09-16 08:00 | 18.5mm | 4 |
| E20251010-01 | 2025-10-10 23:00 | 78.1mm | 5 |
| E20260314-01 | 2026-03-14 07:00 | 14.9mm | 1 |

**诊断结果汇总**：

| 节点类型 | 诊断类别 | 节点数 |
|---------|---------|--------|
| 污水 | 混接 | 2（W13, W15）|
| 污水 | 直连 | 1（W19）|
| 污水 | 入渗 | 1（W17）|
| 污水 | 未定 | 10 |
| 雨水 | 雨水管低效 | 9 |
| 雨水 | 未定 | 3 |

**回归测试**：`make verify-0423` → **3/3 通过**

---

## 四、下一步建议

### 近期（会议准备，2026年7月前）

**① 数据补充与核实（最高优先级）**

- 核查 W20–W24、S11、S12 传感器状态：是否存在设备离线、配置错误，或数据文件命名不匹配
- W16 的混合入流+入渗特征需额外数据支持，当前"未定"对会议汇报影响最大
- 增加更多降雨事件数据（当前仅5场，部分节点因事件覆盖不足无法诊断）

**② 生成会议报告**

```bash
make run   # 运行完整流水线，生成 reports/<date>/report.md 和 report.docx
```
然后将关键图表（节点升幅条形图、诊断类别饼图、事件甘特图）迁移至 PPT。

**③ 看板演示准备**

- 在 Streamlit 看板 Tab 4 中补充诊断解读说明（每种类型的物理含义）
- 检查 Tab 3 热力图在26节点下的显示效果
- 录制 3–5 分钟的流水线演示视频备用

**④ 修复 recession_halflife 对次级降雨的鲁棒性**

在 `recession_halflife()` 内，对 recession 窗口的液位序列先做单调性过滤（去除抬升段），再做指数拟合。这是提升 W15、W13 半衰期计算准确性的最直接路径。

### 中期（方法完善）

**⑤ stuck0 精细化策略**

按节点类型差异化处理 stuck0：雨水节点干季0值保留为 good，避免 S04/S11/S12 等节点 BWF 样本不足。

**⑥ 诊断类别扩展**

为 W16 类节点引入"混合"类别（入渗+入流），或在置信分中区分主次成因，与 0423 报告描述的复杂模式对齐。

**⑦ Thiessen 权重降雨**

`stations_rainfall.yaml` 已有站点-节点映射骨架，后期可基于 Thiessen 多边形为每个节点计算空间权重降雨量，替代当前全区共用代表站的简化方案。

---

## 五、节点 ID 对照表（原编号 → 拼音首字母缩写）

节点展示层不再用 S01/W11 类顺序编号，改用 `sites.yaml` 中已有的位置拼音缩写。原编号仍保留在代码内部作为数据键，对照关系如下：

### 雨水节点（stormwater）

| 原编号 | 新 ID（拼音缩写） | 中文名 | 位置 |
|--------|------------------|--------|------|
| S01 | THBL-RJLLT | 太华北路仁谨路连通管 | 太华北路仁谨路连通管南侧上游 |
| S02 | HQNL-THBL-D | 汉渠南路太华北路东侧 | 汉渠南路与太华北路十字东侧 |
| S03 | HQNL-THBL-B | 汉渠南路太华北路北侧 | 汉渠南路与太华北路十字北侧 |
| S04 | BSH-THL-X | 北三环太华路西侧 | 北三环与太华路十字西侧 |
| S05 | BSH-THL-D | 北三环太华路东侧 | 北三环与太华路十字东侧 |
| S06 | HQDL-D3000 | 红旗东路D3000 | 红旗东路D3000管道 |
| S07 | HQDL-D2200 | 红旗东路D2200 | 红旗东路D2200管道 |
| S08 | JWBZ-XY-D3000 | 截污泵站下游D3000 | 截污泵站下游D3000管道 |
| S09 | HQL-BZXY-3000 | 红旗路泵站下游3000 | 红旗路泵站下游3000管道 |
| S10 | BCL-FCBL-ND | 北辰路凤城八路南侧路东 | 北辰路凤城八路十字南侧路东D1500 |
| S11 *(扩展)* | FCBL-THBL-D | 凤城八路太华北路东侧 | 凤城八路与太华北路十字东侧 |
| S12 *(扩展)* | BSYS-BZCJK | 灞十雨水泵站进场口 | 灞十雨水泵站（进场口） |

### 污水节点（sewage）

| 原编号 | 新 ID（拼音缩写） | 中文名 | 位置 |
|--------|------------------|--------|------|
| W11 | BEH-XJM | 北二环辛家庙立交 | 北二环辛家庙立交 |
| W12 | THBL-FCJL-N | 太华北路凤城九路南侧 | 太华北路凤城九路南侧 |
| W13 | THBL-HQDL-X | 太华北路红旗东路西侧 | 太华北路与红旗东路十字西 |
| W14 | HQDL-JWBZ-SY | 红旗东路截污泵站上游 | 红旗东路截污泵站上游 |
| W15 | JJL-FCJL-N | 经九路凤城九路南侧 | 经九路与凤城九路十字南 |
| W16 | HQDL-GYT-D | 红旗东路广运潭大道东侧 | 红旗东路与广运潭大道十字东侧 |
| W17 | BHXL-CBSL-N | 灞河西路浐灞三路南侧 | 灞河西路浐灞三路十字南 |
| W18 | BCL-BEH-N | 北辰路北二环南侧 | 北辰路北二环十字南侧 |
| W19 | BEH-XJM-SY | 北二环辛家庙立交上游 | 北二环辛家庙立交上游 |
| W20 *(扩展)* | YJL-HQDL-N | 御井路红旗东路南侧 | 御井路与红旗东路十字南侧 |
| W21 *(扩展)* | THBL-BEH-N38 | 太华北路北二环南侧38m | 太华北路北二环十字南侧路口向南38m |
| W22 *(扩展)* | FCBL-THBL-B | 凤城八路太华北路北侧 | 凤城八路与太华北路十字北侧 |
| W23 *(扩展)* | BEH-THL-D | 北二环太华路立交东侧 | 北二环太华路立交东侧 |
| W24 *(扩展)* | BEH-THL-B | 北二环太华路立交北侧 | 北二环太华路立交北侧 |

> **说明**：原编号（S01–S12，W11–W24）继续作为代码内部主键（`node_id`）、数据文件名和回归测试锚点，不做代码级改动。拼音缩写已在 `sites.yaml` 的 `pinyin` 字段中登记，`node_label()` 函数展示时优先取拼音缩写，回退到中文名。

---

## 七、补充分析模块（2026-06-16）

本阶段在六阶段主流水线之外，新增两个独立补充分析模块（均仅针对污水节点），并完成数据质量评估报告。

---

### 1. RTK 单位线模块完善（`src/monitorda/rtk.py`）

上一版本 RTK 仅通过 `node_id.startswith("W")` 的字符串前缀启发式过滤污水节点，本次改为以 `sites.yaml` 中 `kind == "sewage"` 为准。

**关键改进**：

- **污水节点过滤**：`run_rtk()` 改为从 `sites().get("nodes")` 提取 `kind == "sewage"` 节点列表，不再依赖命名规则
- **自适应最小样本量**：新增 `min_obs_per_event` 参数（默认 12）。运行前对每个节点计算事件窗口数据覆盖率；若覆盖率 < 30%，自动降至 4（稀疏节点放宽阈值），避免因数据断档导致所有事件被跳过

---

### 2. NMF 夜间基流分析模块（`src/monitorda/nmf.py`，新建）

新增独立模块，实现 EPA Night Minimum Flow（NMF）方法，以夜间最小液位（NML）为代用指标，量化各污水节点背景入渗对降雨的敏感性。

**方法原理**：

凌晨 02:00–04:00 居民用水量可忽略，此窗口最低液位（Night Minimum Level, NML）可视为管网内背景入渗底线。

NMFD（Night Minimum Flow Deviation）定义为：

```
nmfd_ratio = (NML_雨后 − NML_旱夜) / NML_旱夜
```

| 分类 | 阈值 | 含义 |
|------|------|------|
| `high_infiltration` | NMFD ≥ 30% | 地下水入渗显著，雨后抬升明显 |
| `moderate` | 10% ≤ NMFD < 30% | 中等入渗响应 |
| `low` | -10% ≤ NMFD < 10% | 入渗响应微弱 |
| `negative_response` | NMFD < -10% | 液位计数据水力效应（12-96h 雨后窗口捕捉到回落段），非真实入渗减少 |
| `no_post_data` | 旱夜 ≥ 2 天但雨后夜为 0 | 事件期间设备离线，无法采集雨后数据 |
| `data_insufficient` | 旱夜 < 2 天 | 旱夜数据量不足 |

**关键调试发现**：

实测数据并非严格 10min 等间隔，凌晨 02:00–04:00 窗口内实际最多只有约 3 个点（近似小时级）。最小有效点数阈值 `_MIN_POINTS` 从 4 修正为 2，否则所有节点旱夜数均为 0。

**输出文件**：

- `data/processed/nmf_by_node.parquet`：逐日逐节点 NML 时序
- `data/processed/nmf_summary.parquet`：节点级汇总（NMFD + 分类）

**运行结果（14 个污水节点，2026-06-16）**：

| 节点 | 分类 | NMFD | 旱夜 | 雨后夜 | 备注 |
|------|------|------|------|--------|------|
| W13 | moderate | +25% | 133 | 6 | 与 RDII 入渗诊断一致 |
| W15 | low | -5% | — | — | 入渗响应微弱 |
| W16 | low | -6% | — | — | — |
| W19 | low | 0% | — | — | — |
| W14 | negative_response | -46% | — | 2 | 液位回落段被采集 |
| W17 | negative_response | -33% | — | — | 同上 |
| W11 | no_post_data | — | 83 | 0 | 事件期间设备离线 |
| W12 | no_post_data | — | 37 | 0 | 同上 |
| W18 | no_post_data | — | 105 | 0 | 同上 |
| W20–W23 | no_post_data / data_insufficient | — | — | — | 数据覆盖不足 |
| W24 | data_insufficient | — | 1 | — | 旱夜仅 1 天 |

---

### 3. Streamlit 看板扩展（`app/streamlit_app.py`）

- 流水线控制面板增加 `⑦ RTK分析` 和 `⑧ NMF分析` 两个阶段按钮
- 数据状态栏增加 `RTK 分析` 和 `NMF 分析` 两项
- Tab 4 新增 NMF 可视化区块：节点分类分组条形图 + 逐日 NML 时序图（可按节点选择）+ 方法说明折叠区

---

### 4. 数据质量评估报告（`docs/数据质量评估报告_2026Q2.md`）

针对 2026 年 3 月至今的在线监测数据，起草现场设备排查报告。核心发现：

- **雨量站全面失联**：5 站中 4 站于 2025-10-12 停更；V8870 止于 2026-03-14；2026 年 4 月后完全无降雨记录，事件识别模块全面失效
- **A 级（立即排查）**：W11、S12、W18、W24 近期完全失联；W18 与 W24 同日（2026-05-25）中断，疑共用通信链路
- **B 级（待核实）**：S04、S06 长期 stuck0（雨水管旱季干管可能合理，需现场验证）；W14 stuck0 频繁切换（泵运行周期触发）
- **C 级（远程监控）**：S09 流量计全零、W16 流量计间歇性掉零
- 整改优先级 P0：恢复 V8870 雨量接入 + 联合排查 W18/W24

---

## 六、代码提交记录

| Commit | 说明 |
|--------|------|
| `12f1e1d` | feat: 新增 RTK 单位线拟合模块，定量分离快速入流与慢速入渗 |
| `3c4df34` | fix: 修复事件识别稀疏数据 bug，校准诊断阈值，通过 0423 回归 |
| `636f91c` | feat: 新增 Streamlit 看板，重构入库/事件/诊断模块 |
| `ecc3d2e` | 添加 CLAUDE.md 与 EPA 智慧排水文档知识沉淀 |
| `97c4d4c` | 搭建 monitorda 排水管网监测诊断流水线骨架 |
